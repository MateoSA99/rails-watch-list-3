<h1>Bookmarks#new</h1>
<p>Find me in app/views/bookmarks/new.html.erb</p>
<%= simple_form_for ([@list, @bookmark]) do |f| %>
  <%= f.input :comment %>
  <%# <%= f.select :list, List.all.collect { |x| [x.name, x.id] } %>
  <%# <%= f.select :movie, Movie.all.collect { |x| [x.title, x.id]}  %>
  <%= f.association :movie %>
  <%= f.button :submit %>
<% end %>

